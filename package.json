{
  "name": "medlens",
  "version": "1.0.0",
  "description": "MedLens - See the record. Understand the evidence.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node -e \"const fs=require('fs'); ['index.html','styles.css','app.js'].forEach(f => fs.copyFileSync('public/' + f, f))\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
